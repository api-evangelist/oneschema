{
  "opencollection": "1.0.0",
  "info": {
    "name": "OneSchema AWS Secrets Manager AWS Secrets Manager Accounts Workspace Sheets API",
    "version": "1"
  },
  "request": {
    "auth": {
      "type": "apikey",
      "key": "X-API-KEY",
      "value": "{{X-API-KEY}}",
      "placement": "header"
    }
  },
  "items": [
    {
      "info": {
        "name": "Workspace Sheets",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Assign a template to a Workspace sheet",
            "type": "http"
          },
          "http": {
            "method": "PUT",
            "url": "https://api.oneschema.co/v1/sheets/:sheet_id/template",
            "params": [
              {
                "name": "sheet_id",
                "value": "",
                "type": "path",
                "description": "The id of the sheet."
              }
            ],
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Assign or unassign a template for a Workspace sheet. Pass `template_key: null` to unassign the current template.\n\nSet `auto_set_header_row` to `true` to automatically detect and set the header row before applying the template. If the sheet's headers are already set, `auto_set_header_row` is ignored.\n"
        }
      ]
    }
  ],
  "bundled": true
}